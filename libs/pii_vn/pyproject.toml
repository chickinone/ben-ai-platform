[project]
name = "ben-pii-vn"
version = "0.1.0"
description = "Nhận diện và xử lý PII tiếng Việt cho Bến AI Platform"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
