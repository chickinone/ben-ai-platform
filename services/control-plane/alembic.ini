[alembic]
script_location = %(here)s/migrations
prepend_sys_path = .
# Database URL comes from BEN_MIGRATION_DATABASE_URL (see migrations/env.py).
# Keep this file ASCII-only: Windows reads .ini files with the locale code page, not UTF-8.

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
